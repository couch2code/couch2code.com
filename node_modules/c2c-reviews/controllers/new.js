module.exports = function($scope, submission, $state) {
  $scope.reviews = submission.reviews;
  $scope.state = 'New';
  $scope.save = function(state) {
    $scope.reviews.state = state;
    $scope.reviews.$add($scope.review);
    $state.go('reviews.list');
  }
};
