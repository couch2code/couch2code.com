module.exports = function($scope, review) {
  $scope.review = review;
  $scope.state = 'Edit';
  $scope.save = function(state) {
    $scope.challenge.state = state;
    $scope.challenge.$save();
    $state.go('reviews.list', { submission: review.submission_id });
  }
};
