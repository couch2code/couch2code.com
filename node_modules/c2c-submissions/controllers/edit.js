module.exports = function($scope, challenge, submission) {
  $scope.submission = submission;
  $scope.state = 'Edit';
  $scope.save = function(state) {
    $scope.submission.state = state;
    $scope.submission.$save();
    $state.go('submissions.list', { challenge: challenge});
  }
};
